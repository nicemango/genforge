{
  "$schema": "../config/llm-providers.schema.json",
  "_comment": "Trend Agent 配置 - 模型通过 llm-providers.json 统一管理",

  "provider": "volcengine-kimi",
  "agent": {
    "topic": "ai",
    "maxArticlesPerSource": 20,
    "freshDays": 7,
    "outputDir": "output/trend-agent"
  }
}
